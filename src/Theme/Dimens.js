const { innerWidth, innerHeight } = window;
const headerHeight = 80;
const snackBarHeight = 70;

const dimens = {
  radius: '5px',
  window: {
    width: innerWidth,
    height: innerHeight
  },
  headerHeight,
  snackBarHeight
};

export default dimens;
